# Handoff 2026-06-17

## Current Baseline

Current reportable CoreMark-best strict 50 MHz exact-ROM routed candidate:

| Candidate | LUT | CoreMark/MHz | DMIPS/MHz | Clock | Timing | Status |
| --- | ---: | ---: | ---: | ---: | --- | --- |
| `impl136_lightfold_rc512_tagtrim_routeHigherDelayCost_cpu50_wns+0p017` | 9895 post-route Slice LUTs | 4.287448 | 1.178213 matched xsim | 50 MHz | WNS +0.017 ns / WHS +0.155 ns | accepted routed DCP candidate; bitstream pending |

Current bitstream-backed strict candidate remains:

| Candidate | LUT | CoreMark/MHz | DMIPS/MHz | Clock | Timing | Status |
| --- | ---: | ---: | ---: | ---: | --- | --- |
| `impl104_rc512_dynbht64_strong1_static1_exactrom_cpu50_wns+0p022` | 9694 post-route Slice LUTs | 4.216239 | 2.506614 matched xsim | 50 MHz | WNS +0.022 ns / WHS +0.041 ns | bitstream generated; not board-proven |

Do not report fast-only, synth-only, timing-failed, or demo-ROM results as a
baseline.

## Latest Optimization Batch

| Experiment | Result | Decision |
| --- | --- | --- |
| `fast137_lightfold_rc512_tagtrim_xor1_iter10` | `4.285487 CoreMark/MHz`, CRC `0xfcaf` | rejected; slower than `impl136`, no synth run |
| `fast140_lightfold_rc256_tagtrim_ntfold_fexmem0_iter10` | `4.315138 CoreMark/MHz`, CRC `0xfcaf` | promoted to synthesis audit |
| `synth140_lightfold_rc256_ntfold_fexmem0_reject_wns-4p466` | `9035 synth LUT`, synth `WNS -4.466 ns`, `WHS +0.132 ns` | rejected; timing failed, no implementation run |

The `synth140` hypothesis was that disabling `ENABLE_FOLD_EXMEM_LOAD_USE_SPEC`
could reduce the DCache/MEM-to-front-end path while keeping not-taken-fold
performance. It did not close timing. This confirms that the not-taken-fold
structure still needs RTL retiming or decoupling before it can be considered
again.

## Root Timing Picture

- `impl136` closes post-route timing, but with small margin: `WNS +0.017 ns`.
- Its worst path is route-dominated and runs from DCache tag state toward ID/EX
  control/data registers, not the original 41-level PC-select path.
- Directly enabling not-taken fold reintroduces a DCache/MEM-to-front-end
  combination path into IF/ID instruction selection.
- RC256 not-taken fold has attractive score and area, but fails synthesis timing
  even with fold EX/MEM load-use forwarding disabled.

## Do Not Repeat

- Do not retry `REDIRECT_CACHE_XOR_INDEX=1` on the current `impl136` family
  unless the RTL changes first; measured result was lower at `4.285487`.
- Do not directly enable `ENABLE_ID_BRANCH_NOT_TAKEN_FOLD=1` in the current
  same-cycle form; `synth132`, `synth133`, and `synth140` all fail timing.
- Do not use old `fast115`/`fast121` high-fold scores as candidate evidence;
  matching implementation failed badly.

## Recommended Next Work

1. Keep `impl136` as the current reportable strict routed candidate.
2. If optimizing performance, work on an RTL-decoupled not-taken-fold variant:
   register the not-taken fold request and/or next-cache lookup so DCache/MEM
   state does not feed IF/ID instruction or PC selection in the same cycle.
3. Before any full implementation, run strict CoreMark fast gate and synthesis.
   Only promote to route if CoreMark/MHz stays above the current target and
   synthesis timing is near closure.
4. After the candidate is stable, generate `impl136` or successor bitstream,
   then collect PROGRAM_OK, UART capture, and board video evidence.

## Resume Checklist

Run first:

```powershell
git status --short --branch --untracked-files=no
```

Known unrelated dirty file remains:

- `YH_rv_cpu/scripts/resolve_python.bat`

Do not bulk-stage historical untracked experiment files. Stage only files
explicitly related to the handoff or the active optimization batch.
