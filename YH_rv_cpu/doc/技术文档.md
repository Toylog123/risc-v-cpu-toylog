# YH_rv_cpu 技术文档

## 1. 文档范围

本文件是 `YH_rv_cpu` 当前比赛提交主线的技术基线说明。目标是提供一份和当前脚本、RTL、TB、报告、交付材料一致的单一入口，不再保留会误导当前基线的旧口径。

当前基线日期：`2026-04-04`

## 2. 工程目标

- 形成可复现的比赛提交级 CPU 工程基线
- 保持 RTL、脚本、验证、FPGA 构建与文档口径一致
- 在严格控制回归风险的前提下继续做小步优化

## 3. 设计总览

### 3.1 CPU 核心

- 指令集主线：`RV32I + Zicsr`
- 共线验证：`RV64` baseline regression
- 流水线：`IF / ID / EX / MEM / WB`
- 主要机制：
  - EX/MEM、MEM/WB 前递
  - branch / jump redirect
  - load-use hazard 检测
  - CSR / trap / machine timer interrupt

### 3.2 SoC 组成

- 指令存储：`rtl/YH_rv_sync_imem_rom.v`
- 数据存储：`rtl/YH_rv_dmem_ram.v`
- SoC 顶层：`rtl/YH_rv_cpu_soc.v`
- CPU 顶层：`rtl/YH_rv_cpu.v`
- FPGA 顶层：`fpga/vivado/src/YH_rv_cpu_fpga_top.v`
- 外设：
  - UART
  - timer
  - DONE flag

### 3.3 FPGA 默认内存路径

当前冻结的 FPGA 默认配置为：

```verilog
parameter integer IMEM_OUTPUT_REG = 0;
parameter integer DMEM_OUTPUT_REG = 0;
```

该默认值已经通过 fresh `impl50` 和 FPGA-like CoreMark probe 验证。

## 4. 当前冻结基线

### 4.1 比赛提交主线

| 项目 | 当前结果 |
|------|----------|
| CoreMark short score | `0.912472 CoreMark/MHz` |
| CoreMark short completion cycles | `11014885` |
| CoreMark strict score | `0.912465 CoreMark/MHz` |
| CoreMark strict completion cycles | `1095991523` |
| CoreMark strict runtime | `10.959325s` |
| CoreMark score 口径 | short path `competition_reportable=yes` |
| strict 结论 | `strict_eembc_10s_compliant=yes` |
| CoreMark smoke | `620530 cycles` |
| RV32 baseline | `33/33` |
| RV64 baseline | `21/21` |
| impl50 资源 | `2555 LUT / 2170 FF / 4 BRAM / 0 DSP` |
| impl50 时序 | `WNS = +5.822ns`，`WHS = +0.057ns` |
| FPGA-like probe | `156442 cycles`，`7.728811 CoreMark/MHz` |

### 4.2 100MHz 参考实现

`100MHz` 结果仅作为参考实现保留，不是当前比赛冻结提交口径。

| 项目 | 结果 |
|------|------|
| Slice LUTs | `2598` |
| Slice Registers | `2240` |
| BRAM | `4` |
| DSP | `0` |
| Setup WNS | `+0.062ns` |
| Hold WHS | `+0.048ns` |

注意：不要再把历史 `async_default` 子路径值当作当前顶层 `100MHz` 总结值。

## 5. 当前保留优化

### 5.1 O1：同步 load hazard 收紧

- RTL 位置：`rtl/YH_rv_cpu_hazard_unit.v`
- 当前保留策略：`stall_decode = load_use_hazard`
- 收益：
  - 当前冻结 short score 为 `0.912472`
  - 当前 strict valid score 为 `0.912465`
  - `riscv-tests rv32` / `rv64` baseline 无回归

### 5.2 O2：FPGA 默认内存路径收紧

- RTL 位置：`fpga/vivado/src/YH_rv_cpu_fpga_top.v`
- 当前保留默认值：
  - `IMEM_OUTPUT_REG=0`
  - `DMEM_OUTPUT_REG=0`
- 收益：
  - `impl50` fresh 结果保持通过
  - FPGA-like CoreMark probe 达到 `156442 cycles`，`7.728811 CoreMark/MHz`

### 5.3 明确不保留的方向

- 不能简单通过放松 `stall_decode` 门控继续做 fetch 前端提分
- 如需继续前端优化，必须转为 `fetch/request/queue` 解耦方向单列实验

## 6. 验证矩阵

### 6.1 当前主线验证命令

```bat
scripts\run_coremark_smoke.bat rv32
scripts\run_coremark_score.bat rv32 10 2000 100000000UL 20000000
scripts\run_riscv_tests_subset.bat rv32
scripts\run_riscv_tests_subset.bat rv64
scripts\build_vivado_project.bat impl50
scripts\run_coremark_fpga.bat rv32
```

### 6.2 当前结论

| 验证项 | 结果 | 备注 |
|--------|------|------|
| CoreMark score short | 通过 | `competition_reportable=yes` |
| CoreMark score strict | 通过 | `strict_eembc_10s_compliant=yes` |
| CoreMark smoke | 通过 | `620530 cycles` |
| riscv-tests rv32 | 通过 | baseline `33/33` |
| riscv-tests rv64 | 通过 | baseline `21/21` |
| impl50 | 通过 | 生成 bitstream |
| FPGA-like probe | 通过 | tuning-only probe |

## 7. FPGA pre-board 与 board-ready 状态

### 7.1 当前已经收口

- `impl50` 是当前比赛面对的冻结构建入口
- 串口口径固定为 `115200 8N1`
- bitstream 路径固定为 `project/YH_rv_cpu_nexys_a7_100_20p000.bit`
- 证据归档目录固定为 `doc/fpga_bringup_evidence/<YYYY-MM-DD>/`
- demo firmware staging SOP 已写入 `doc/fpga_bringup_checklist.md`

### 7.2 当前还不能宣称完成的项

- 仍缺实体板卡，无法完成 UART/LED 实板闭环
- 当前 `XDC` 仍缺板级 I/O delay 约束，不能宣称 board-grade signoff 完成

## 8. 关键文件

| 文件 | 作用 |
|------|------|
| `rtl/YH_rv_cpu.v` | CPU 顶层 |
| `rtl/YH_rv_cpu_soc.v` | SoC 顶层 |
| `rtl/YH_rv_cpu_hazard_unit.v` | hazard 控制 |
| `rtl/YH_rv_sync_imem_rom.v` | 指令 ROM |
| `rtl/YH_rv_dmem_ram.v` | 数据 RAM |
| `fpga/vivado/src/YH_rv_cpu_fpga_top.v` | FPGA 顶层 |
| `scripts/run_coremark_score.bat` | 冻结 CoreMark score 入口 |
| `scripts/run_coremark_fpga.bat` | FPGA-like probe 入口 |
| `scripts/build_vivado_project.bat` | Vivado 构建入口 |

## 9. 当前阻塞

### 9.1 本机内仍在推进

- 第二轮优化前基线冻结
- `fetch/request/queue` 解耦方向设计与实验

### 9.2 外部阻塞

- 实体板卡未到位
- 最终板级 I/O delay 约束与实板证据缺失

## 10. 下一轮优化约束

只有在以下条件都满足后，才允许进入第二轮性能优化：

1. strict CoreMark 口径已收口
2. 当前文档已同步
3. 工作区调试资产已治理
4. 新基线 fresh 回归已完成

优化阶段必须遵守：

- 单变量、小步、可回归
- 每轮都完整重跑：
  - CoreMark score
  - CoreMark smoke
  - RV32 baseline
  - RV64 baseline
  - impl50
