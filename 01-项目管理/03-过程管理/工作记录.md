# 工作记录

> 更新时间：`2026-03-16`

## 2026-03-16

### 事项 1：锁定主赛题

- 结论：七星微已作为唯一主赛题，不再并行推进其他企业命题。

### 事项 2：建立正式比赛工程

- 结论：正式工程固定命名为 `YH_rv_cpu`。
- 结果：目录、脚本、文档和工具链入口统一收口。

### 事项 3：打通五级流水和最小 SoC

- 结论：当前已有 `RV32I` 五级流水第一版和最小 SoC。
- 结果：能通过 UART 输出 `YH_rv_cpu boot`。

### 事项 4：补齐最小机器态 `CSR / trap`

- 结论：最小机器态 trap 路径已经可用。
- 结果：trap 烟测通过。

### 事项 5：补齐 machine timer interrupt

- 结论：当前已经能响应 machine timer interrupt 并 `mret` 返回。
- 结果：timer irq 烟测通过。

### 事项 6：整理交接与协作机制

- 结论：当前已形成统一的项目管理、工程交接、任务清单和默认同步规则。

### 事项 7：确定下一阶段主线

- 结论：下一阶段主线是 `RV32 / RV64` 共线、`riscv-tests`、`CoreMark` 和 `Vivado/FPGA`。

### 事项 8：抽出 `XLEN` 参数化骨架

- 结论：CPU 顶层、SoC 顶层、关键流水级、ALU 和寄存器堆已经抽出 `XLEN`。
- 结果：当前 `RV32` 烟测继续通过，为后续 `RV64` 演进打下基础。

### 事项 9：建立 `XLEN=64` 基础烟测

- 结论：已经新增独立的 `xlen64` 烟测，验证 64 位数据通路和大于 31 的移位量。
- 结果：`PASS: xlen64 smoke test completed at PC=0000000000000020 in 17 cycles`

### 事项 10：整理 Vivado 本地工程目录

- 结论：Vivado 工程和中间产物统一收口到根目录 `project/`。
- 结果：根目录不再保留 `.Xil`、`tclapp`、`vivado.log`、`vivado.jou` 这类过程文件。

### 事项 11：补工具链本地路径回退

- 结论：脚本现在会先查 `PATH`，找不到再查本机 `xPack` 和 `scoop` 安装目录。
- 结果：`check_toolchain.bat` 和 `build_firmware.bat` 已能在当前机器上正常找到 `iverilog`、`riscv-none-elf-*`。

### 事项 12：继续推进 FPGA 综合链

- 结论：`YH_rv_cpu_soc` 的 RAM 写法已调整到可进入综合，之前阻塞综合的 RAM 推断错误已消失。
- 结果：Vivado 已能完成到综合主阶段前半程，但报告导出还需要继续收口。

### 事项 13：扩大验证闭环

- 结论：最小 `riscv-tests` 回归已经重新验证通过。
- 结果：`PASS: riscv-tests finished at PC=0000059c in 495 cycles with tohost=1`

### 事项 14：打通 Vivado 本地综合链

- 结论：Vivado 现在可以从当前中文路径工作区稳定进入综合，并导出资源/时序报告。
- 结果：
  - `build_vivado_project.bat` 现在会自动临时映射 ASCII 盘符
  - `build_nexys_a7_100_project.tcl` 已增加分步日志
  - 已稳定导出 `project/reports/synth_utilization.rpt`、`project/reports/synth_timing_summary.rpt` 和 `project/YH_rv_cpu_nexys_a7_100_synth.dcp`

### 事项 15：拿到第一版 FPGA 资源与时序结论

- 结论：当前器件资源不是主要风险，现阶段主要风险在时序收敛和正式板级约束。
- 结果：
  - 资源：`Slice LUTs = 3445`，`Slice Registers = 1962`，`BRAM = 0`，`DSP = 0`
  - 时序：`100MHz` 下 `WNS = -2.405ns`
  - 当前模板 `XDC` 仍有 `no_input_delay(1)` 和 `no_output_delay(4)`，说明正式板级约束还没冻结

### 事项 16：补本地 Vivado 清理脚本

- 结论：当前已增加本地清理脚本，后续不会再让 `project/` 积累过多无用中间文件。
- 结果：新增 `YH_rv_cpu/scripts/clean_vivado_project.bat`

### 事项 17：补齐 50MHz / 100MHz 双档 FPGA 综合结论

- 结论：Vivado 现在已经可以稳定输出双档频率报告，比赛目标频率和继续优化目标被正式拆开。
- 结果：
  - 新增脚本入口：`build_vivado_project.bat synth50`、`build_vivado_project.bat synth100`
  - 新报告目录：
    - `project/reports/clk_10p000ns`
    - `project/reports/clk_20p000ns`
  - 资源：
    - `100MHz`：`3450 LUT / 1962 FF / 0 BRAM / 0 DSP`
    - `50MHz`：`3424 LUT / 1962 FF / 0 BRAM / 0 DSP`
  - 时序：
    - `100MHz`：`WNS = -2.487ns`
    - `50MHz`：`WNS = 7.525ns`
  - 关键判断：50MHz 已经满足比赛目标，100MHz 仍需继续收敛
