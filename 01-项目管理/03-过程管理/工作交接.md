# 工作交接

> 更新时间：`2026-03-16`
> 当前项目：七星微赛题 `YH_rv_cpu`

## 先说结论

- 赛题已经锁定，不再做选题比较。
- 当前工程已经不是空骨架，已有五级流水、最小 SoC、最小机器态 trap 和 machine timer interrupt 闭环。
- 当前已经抽出 `XLEN` 参数化骨架。
- 当前已经有独立的 `XLEN=64` 基础烟测。
- 当前 `riscv-tests` 子集已经接入并跑通最小回归。
- Vivado 本地工程目录已经固定为根目录 `project/`，不进入 Git 同步。
- 现在最该做的不是继续堆新模块，而是补齐 `RV64` 语义、验证、跑分和 FPGA。

## 当前项目状态

- 正式工程：`../../YH_rv_cpu/`
- 当前 CPU 基线：自写 `RV32I + Zicsr` 五级流水
- 当前 SoC 状态：最小 SoC 已打通，可串口输出 `YH_rv_cpu boot`
- 当前验证状态：
  - `SoC smoke` 通过
  - `trap smoke` 通过
  - `timer irq smoke` 通过
  - `xlen64 smoke` 通过
  - `riscv-tests rv32 add` 通过
- 当前 FPGA 状态：
  - 本地 Vivado 工程脚手架已建立
  - RAM 写法已调整到可进入综合
  - `build_vivado_project.bat synth` 已能稳定导出资源/时序报告
  - 当前本地综合默认会临时映射 ASCII 盘符，规避中文路径导致的 Vivado 退出问题
  - 当前第一版综合结果：`3445 LUT / 1962 FF / 0 BRAM / 0 DSP`
  - 当前 100MHz 约束下 `WNS = -2.405ns`

## 当前最缺的内容

- `RV64` 指令级扩展和验证
- `riscv-tests` 覆盖面继续扩大
- `CoreMark`
- 正式板级约束、时序收敛和上板流程

## 接手时先看哪些文件

1. `../01-赛题要求/七星微赛题要求.md`
2. `../01-赛题要求/七星微赛方答疑整理.md`
3. `../02-项目规划/项目总体规划.md`
4. `任务清单.md`
5. `../../YH_rv_cpu/doc/YH_rv_cpu_handoff.md`
6. `../../YH_rv_cpu/doc/技术文档.md`
7. `../../YH_rv_cpu/doc/YH_rv_cpu_preliminary_design.md`

## 默认同步范围

- `YH_rv_cpu`
- `04-工具链`
- `01-项目管理`

默认暂存脚本：

- `../../YH_rv_cpu/scripts/stage_default_sync.bat`

## 下一步顺序

1. 在 `XLEN` 骨架和 `xlen64` 烟测基础上继续补 `RV64` 译码和访存
2. 扩大 `riscv-tests` 回归并整理结果
3. 接 `CoreMark`
4. 继续做 Vivado 时序收敛和正式板级约束
5. 拿到板卡后冻结正式 `XDC` 并推进上板

## 交接要求

- 每次有实际进展，都同步更新：
  - `工作记录.md`
  - `工作交接.md`
  - `任务清单.md`
- `YH_rv_cpu` 内的修改，同时更新：
  - `../../YH_rv_cpu/doc/YH_rv_cpu_handoff.md`
  - `../../YH_rv_cpu/doc/YH_rv_cpu_change_log.md`
  - `../../YH_rv_cpu/doc/YH_rv_cpu_todo.md`

## 2026-03-17 追加结论

- Vivado 综合口径已经从单一 `100MHz` 扩成双档：
  - `project/reports/clk_10p000ns`
  - `project/reports/clk_20p000ns`
- 最新结果：
  - `100MHz`：`3450 LUT / 1962 FF / 0 BRAM / 0 DSP`，`WNS = -2.487ns`
  - `50MHz`：`3424 LUT / 1962 FF / 0 BRAM / 0 DSP`，`WNS = 7.525ns`
- 当前最适合对老师和队友说的真实口径：
  - 资源不是当前主风险
  - 50MHz 已经有把握
  - 100MHz 还要继续收敛
  - 板卡和正式 `XDC` 仍然是下一阶段关键依赖
- 当天又试了一轮更激进的寄存器使能链优化，但 100MHz 结果更差，已经回退到稳定基线
- 当前关于 BRAM 的真实交接结论：
  - `ROM/RAM` 还没上 BRAM，不是属性没写够
  - 根因是当前 SoC 的取指和 load 仍假设零等待、组合读返回
  - 后续要做 BRAM 化，必须先改 `imem/dmem` 的同步返回语义
## 2026-03-17 继续推进补充

- 这轮已经把同步取指接入主线，不再只是“后续计划”：
  - `rtl/YH_rv_cpu.v`
  - `rtl/YH_rv_cpu_soc.v`
  - `rtl/YH_rv_sync_imem_rom.v`
- 固件和测试脚本现在会额外生成 `*.mem32.hex`
- Vivado 现在会优先吃 `current.hex/current.mem32.hex`
- 最新综合结果：
  - `100MHz`：`4086 LUT / 2040 FF / 1024 LUTRAM / 0 BRAM / 0 DSP`，`WNS = -2.468ns`
  - `50MHz`：`4061 LUT / 2040 FF / 1024 LUTRAM / 0 BRAM / 0 DSP`，`WNS = 7.548ns`
- 盘符映射逻辑也加固了，后续如果 `V:` 被占用，会继续尝试 `W:/X:/Y:/Z:`，不再直接退回中文路径
- 当前最值得继续的方向已经更聚焦：
  - 推进 `dmem` 同步返回语义
  - 评估双口 `ROM` 或独立只读数据路径
## 2026-03-17 当前解读

### 当前项目到了哪一步

- 项目已经从“CPU 骨架搭建”进入“存储结构和 FPGA 口径继续收口”的阶段。
- 当前不是缺主线功能，而是需要把已经跑通的主线继续做扎实：
  - CPU 五级流水稳定
  - 最小 SoC 稳定
  - 基础验证稳定
  - FPGA 综合稳定

### 当前最重要的判断

- 比赛要求的 `50MHz` 口径已经稳定可用。
- `100MHz` 仍是继续优化目标，不是当前交付阻塞点。
- 同步取指已经接入主线，但 `BRAM` 还没有真正被推出来。

### 现在应该怎么向队友或老师解释

- 不是“我们还在搭基础环境”。
- 也不是“已经完全做完 FPGA 优化”。
- 更准确的说法是：
  - 工程主线已经跑通
  - 当前验证链和综合链已经可信
  - 现在正在继续把存储结构往更正式的 FPGA 实现推进

### 当前统一口径

- 最新综合结果以当前测试镜像口径为准：
  - `100MHz`：`4086 LUT / 2040 FF / 1024 LUTRAM / 0 BRAM / 0 DSP`，`WNS = -2.468ns`
  - `50MHz`：`4061 LUT / 2040 FF / 1024 LUTRAM / 0 BRAM / 0 DSP`，`WNS = 7.548ns`
- 当前最真实的结论是：
  - `50MHz` 可以作为比赛演示频率口径
  - `100MHz` 继续优化
  - `BRAM` 仍需后续结构化推进

### 下一步工作解读

- 下一步不是再加一堆新功能。
- 下一步是继续推进：
  1. `dmem` 同步返回语义
  2. 独立存储包装层
  3. `BRAM` 推断
  4. `100MHz` 时序收敛

### 当前回归口径补充

- 当前默认 `rv32` 回归子集已经整组通过，不再只是单个 `add` 用例通过。
- 这意味着现在可以把“默认 `rv32` 子集通过”当作新的功能基线。
- 对外解释时可以直接说：
  - 主线功能已经有成组回归支撑
  - 后续重点转向 `rv64`、存储结构和 FPGA
## 2026-03-17 追加：同步数据路径已接入主线

- 当前 CPU/SoC 主线已经不再依赖纯组合 `dmem` 读返回。
- 已完成：
  - CPU 增加 `dmem_read_req / dmem_rvalid`
  - SoC 增加 `SYNC_DMEM`
  - SoC smoke、trap、timer irq、FPGA top 已切到同步数据路径
- 当前验证：
  - `run_soc_smoke.bat` 通过
  - `run_trap_smoke.bat` 通过
  - `run_timer_irq_smoke.bat` 通过
  - `run_xlen64_smoke.bat` 通过
  - `run_riscv_tests_subset.bat rv32` 通过
- 当前 FPGA 口径：
  - `50MHz`：`3692 LUT / 2069 FF / 1024 LUTRAM / 0 BRAM / 0 DSP`，`WNS = 7.553ns`
  - `100MHz`：`3713 LUT / 2066 FF / 1024 LUTRAM / 0 BRAM / 0 DSP`，`WNS = -2.475ns`
- 接手后优先继续：
  1. 存储包装层拆分
  2. 数据 RAM BRAM 化
  3. `100MHz` 时序收敛

## 2026-03-17 追加：dmem BRAM 已落地

- 这一轮已经把数据 RAM 从 SoC 内联数组推进成独立包装层，并且 Vivado 已实际推成块 RAM。
- 新增：
  - `YH_rv_cpu/rtl/YH_rv_dmem_ram.v`
- 关键结论已经变化为：
  - `dmem` BRAM 路线已验证成功
  - 当前不再是“怎么把 dmem 推成 BRAM”这个问题
  - 当前真正的后续问题变成：
    1. `imem/ROM` 怎么继续收成更合适的存储结构
    2. `100MHz` 怎么借助 BRAM 输出寄存器继续优化
- 最新统一口径：
  - `50MHz`：`2590 LUT / 2033 FF / 2 BRAM / 0 DSP`，`WNS = 7.556ns`
  - `100MHz`：`2611 LUT / 2030 FF / 2 BRAM / 0 DSP`，`WNS = -2.470ns`
- 接手后优先顺序改成：
  1. 推进 `imem/ROM` 包装层
  2. 评估 `dmem` BRAM 输出寄存器
  3. 继续压 `100MHz`
  4. 在新资源口径下继续做 `rv64` / `CoreMark`

## 2026-03-17 当前交接摘要（直接接手版）

### 1. 项目是什么

- 项目已经锁定为七星微赛题正式工程 `YH_rv_cpu`
- 当前不是选题阶段，也不是纯文档阶段，而是“工程主线已跑通，继续做验证、FPGA 和交付收口”的阶段

### 2. 当前做到哪一步

- CPU 五级流水、最小 SoC、trap、timer irq 已经稳定
- `xlen64 smoke` 已经有基础基线
- `riscv-tests rv32` 默认子集已整组通过
- `dmem` 已经从 SoC 内联数组推进到独立包装层，并成功推成 `2 BRAM`
- 当前最新 FPGA 统一口径：
  - `50MHz`：`2590 LUT / 2033 FF / 2 BRAM / 0 DSP`，`WNS = 7.556ns`
  - `100MHz`：`2611 LUT / 2030 FF / 2 BRAM / 0 DSP`，`WNS = -2.470ns`

### 3. 已完成的关键工作

- 同步 `imem` 已接入主线
- 同步 `dmem` 已接入主线
- Vivado `synth50 / synth100` 已稳定输出报告
- `dmem` BRAM 路线已经验证成功
- 技术文档、工作记录、任务清单、项目交接文档已同步到最新口径

### 4. 当前阻塞与风险

- `100MHz` 还没过，但不影响当前 `50MHz` 的比赛口径
- `imem/ROM` 仍主要在 LUT，后续还有优化空间
- `CoreMark` 还没形成稳定结果
- `RV64` 还没有扩成稳定回归集
- 还没有正式板卡与正式 `XDC`

### 5. 下一步最值得做的 3 到 5 项

1. 推进 `imem/ROM` 包装层
2. 评估 `dmem` BRAM 输出寄存器，继续压 `100MHz`
3. 在新资源口径下补 `CoreMark`
4. 扩大 `RV64` / 更完整 `riscv-tests`
5. 板卡到位后冻结正式 `XDC` 并推进上板

### 6. 关键文档与命令

- 项目总体状态：`../../YH_rv_cpu/doc/技术文档.md`
- 当前技术交接：`../../YH_rv_cpu/doc/YH_rv_cpu_handoff.md`
- 当前任务：`任务清单.md`
- 当前记录：`工作记录.md`
- 常用命令：
  - `../../YH_rv_cpu/scripts/check_syntax.bat`
  - `../../YH_rv_cpu/scripts/run_soc_smoke.bat`
  - `../../YH_rv_cpu/scripts/run_trap_smoke.bat`
  - `../../YH_rv_cpu/scripts/run_timer_irq_smoke.bat`
  - `../../YH_rv_cpu/scripts/run_riscv_tests_subset.bat rv32`
  - `../../YH_rv_cpu/scripts/build_vivado_project.bat synth50`
  - `../../YH_rv_cpu/scripts/build_vivado_project.bat synth100`

### 7. 文档缺口与建议补齐项

- 缺正式板卡 `XDC` 与上板日志
- 缺 `CoreMark` 稳定跑分记录
- 缺 `RV64` 扩展后的回归摘要
