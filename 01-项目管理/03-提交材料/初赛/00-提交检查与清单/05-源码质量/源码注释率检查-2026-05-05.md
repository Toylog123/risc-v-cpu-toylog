# 源码注释率检查

检查时间：`2026-05-05`

检查对象：`源代码/CICC1003618_初赛_源代码/`

说明：本检查用于初赛提交前的风险控制。统计采用“注释行 / 总行数”的保守口径，并同步给出“注释行 / 非空行数”的参考口径。此次仅补充提交脚本中的说明性注释，不修改 RTL、TestBench、软件或脚本执行逻辑。

| 统计口径 | 文件范围 | 文件数 | 总行数 | 非空行数 | 注释行 | 注释/总行 | 注释/非空行 |
|---|---|---:|---:|---:|---:|---:|---:|
| 保守提交口径 | `.v/.vh/.sv/.c/.h/.S/.s/.asm/.bat/.cmd/.tcl/.xdc/.py/.ps1/.ld` | `145` | `30409` | `27438` | `9690` | `31.87%` | `35.32%` |
| RTL/TestBench/Verilog 口径 | `.v/.vh/.sv` | `70` | `17976` | `16354` | `5433` | `30.22%` | `33.22%` |

## 结论

1. 按保守提交口径复算，当前源码注释率为 `31.87%`，满足“不低于 30%”要求。
2. 按非空行参考口径复算，当前源码注释率为 `35.32%`，说明注释余量不是由空行稀释形成。
3. RTL/TestBench/Verilog 类文件复算为 `5433 / 17976 = 30.22%`，核心硬件代码也已达到 30% 以上。

## 本次补强位置

| 文件 | 动作 | 影响 |
|---|---|---|
| 提交源码目录内 `145` 个源码/脚本/约束文件 | 增加统一提交上下文头部，说明文件角色、冻结目标、能力边界、验证证据和维护要求 | 只增加注释，不改变执行逻辑 |
| `scripts/check_syntax.ps1` | 保留语法检查脚本用途、短路径映射原因、退出码传递说明 | 只增加注释，不改变执行逻辑 |
| `scripts/render_vcd_waveform_png.py` | 保留波形证据生成用途、信号选择、图像归档与审稿注意事项 | 只增加注释，不改变执行逻辑 |

## 复验记录

| 检查项 | 结果 |
|---|---|
| 语法检查 | `powershell -ExecutionPolicy Bypass -File .\scripts\check_syntax.ps1`，退出码 `0` |
| Zmmul 最小回归 | `cmd /c scripts\run_zmmul_test.bat`，输出 `Zmmul diagnostic: PASS` |
