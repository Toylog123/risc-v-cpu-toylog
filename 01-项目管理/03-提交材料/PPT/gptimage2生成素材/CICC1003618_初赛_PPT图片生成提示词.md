# CICC1003618 初赛 PPT 图片生成提示词

统一要求：每页生成一张 16:9 PPT 页面图，蓝白灰科技汇报风，白色或极浅蓝灰背景，深蓝标题，科技蓝重点数字，深灰正文，浅蓝灰表格和细线。不要出现学校、学院、老师、个人身份信息。不要使用赛博风、强渐变、装饰炫光或多色卡片。中文正文尽量短，英文和数字使用 Times New Roman 风格。

## 第 1 页：封面

生成正式学术报告封面风格 PPT 页面，顶部居中放官方 LOGO 占位，标题为“基于 RISC-V 的五级流水 CPU 设计与 FPGA 验证”，下方表单式细横线列出“报告类型：初赛作品汇报”“参赛杯赛：第十届全国大学生集成电路创新创业大赛”“队伍编号：CICC1003618”“团队名称：芯银河战队”。白底深蓝字，庄重克制。

## 第 2 页：快速预览 1 - 关键指标

生成技术指标总览页，左侧一句“面向 PYNQ-Z2 的轻量级高性能 RISC-V 软核 CPU”，右侧开放式大数字矩阵：4.137461 CoreMark/MHz、2.908287 DMIPS/MHz、4961 LUT、50.0 MHz、PROGRAM_OK + 实时 UART 指标流。底部用细线串联“仿真、性能、资源、时序、上板”证据链。

## 第 3 页：快速预览 2 - 核心亮点

生成核心亮点页，中间是简化五级流水 CPU 方框，四周四组亮点：Zmmul / Zba / Zbb / Zbs 热点增强，JAL early redirect 与 forwarding / stall / flush 协同，LUT < 5000 低资源策略，RTL / TestBench / CoreMark / Vivado / PYNQ-Z2 验证闭环。

## 第 4 页：赛题理解与设计目标

生成赛题约束到设计选择的对照页，左列为“RV32I/RV64I、FPGA 原型、50 MHz、仿真与 FPGA 数据、源码注释率、资源约束”，右列为“RV32I 五级流水、PYNQ-Z2、50 MHz、双基准测试、参数化扩展、LUT < 5000”。中间用细箭头连接。

## 第 5 页：总体架构

生成 SoC 总体架构图，模块包括 YH_rv_cpu_fpga_top、YH_rv_cpu_soc、YH_rv_cpu、IF/ID/EX/MEM/WB、ROM、RAM、MMIO、UART TX、LED。白底深蓝边框，存储和外设用浅蓝灰填充，箭头表示指令流、数据流和外设输出。

## 第 6 页：五级流水与控制通路

生成五级流水控制图，横向排列 IF、ID、EX、MEM、WB，叠加 stall、flush、forwarding、redirect 控制线。突出流水线正确性和性能优化，不放过多信号。

## 第 7 页：指令集与热点扩展

生成 ISA 扩展说明页，中心为 RV32I 基础块，旁边标出 Zmmul、Zba/Zbb/Zbs 进入提交路径；Zbc、XThead、IDBR 作为探索路径以浅灰虚线表示。强调热点扩展与资源可控。

## 第 8 页：流水线难点与解决方案

生成难点解决方案页，三列结构：数据相关、控制相关、资源约束。对应方案为 forwarding + stall、JAL early redirect + branch redirect、参数化关闭高成本路径。蓝白灰，细线连接问题与方案。

## 第 9 页：FPGA 适配与低资源策略

生成 FPGA 适配页，左侧 PYNQ-Z2 / xc7z020clg400-1 / 125 MHz to 50 MHz 时钟路径，右侧低资源策略：顺序五级流水、Zmmul 使用 DSP、可关闭扩展、ROM/RAM 片上存储。底部强调 4961 LUT。

## 第 10 页：验证体系

生成分层验证体系页，四层从下到上：指令/模块定向测试、SoC smoke、CoreMark / Dhrystone 性能基准、Vivado 资源时序与 PYNQ-Z2 上板。最终上板层用深蓝边框强调。

## 第 11 页：关键波形与时序证据

生成关键波形证据页，三张横向波形小图占位，标题分别为“ID 分支快速重定向”“JAL 快速重定向”“Load-use 快速路径”。每张图只放重点信号和两条短注释，用深蓝箭头标注观察点。

## 第 12 页：性能结果

生成双指标性能结果页，左侧 CoreMark：4.137461 CoreMark/MHz、2416941 ticks、413.746136 iter/s；右侧 Dhrystone：2.908287 DMIPS/MHz、510986 Dhrystones/s、10 runs。底部写“CPU 50.0 MHz；结果来自脚本日志、仿真输出和验证报告”。

## 第 13 页：优化路径与收益拆解

生成蓝色科技风优化路径拆解页，左侧是从早期基线到冻结提交再到探索路径的阶梯图，标注 0.912472、4.060276、4.137461、5.162186 CoreMark/MHz；右侧是优化贡献清单，包含 Zmmul、Zba/Zbb/Zbs、JAL early redirect、Forwarding/stall/flush、Dhrystone 构建与库函数路径；底部用细线说明 LUT < 5000、50 MHz、可上板、可复现是最终冻结原则。冻结提交为深蓝，高性能探索为虚线并标注资源超界。

## 第 14 页：FPGA 适配与上板证据

生成 FPGA 上板证据页，左侧留出清晰 16:9 视频嵌入框，框内文字为“演示视频位置：Vivado 下载 + PYNQ-Z2 连线 + UART 实时输出”，使用深蓝细边框、浅蓝灰填充和简洁播放图标；右侧放资源时序表：4961 LUT、2367 FF、6 BRAM、15 DSP、WNS +0.338 ns、WHS +0.059 ns、CPU 50.0 MHz；底部放 PROGRAM_OK 和 YH_rv_cpu UART 指标流日志摘录。

## 第 15 页：AI 工具使用声明

生成 AI 工具使用声明页，两列表格：OpenAI 用于工程脚本梳理、热点路径分析、快速验证流程组织和日志核对；DeepSeek 用于文稿润色、章节逻辑检查和表达压缩。底部声明“核心 CPU 架构、RTL 最终确认、测试结论和指标判定均由团队完成”。

## 第 16 页：总结

生成总结页，中心大字为“高性能、低资源、可复现、可上板”，下方三条总结：完成 RV32I 五级流水 CPU 与 PYNQ-Z2 原型；在 LUT < 5000 与 50 MHz 约束内达到 4.137461 CoreMark/MHz 和 2.908287 DMIPS/MHz；形成完整提交证据链。右下角加“后续可开源复现”，底部串联技术说明书、性能与验证报告、PPT、源代码、FPGA 原型系统、演示视频。
