# 图 1 系统总体架构图

## 最终提示词

```text
请生成一张用于正式初赛设计说明书的系统总体架构图，输出为高分辨率白底 PNG，建议分辨率 2200x1400，横向构图，扁平矢量风，学术技术文档风格，深灰细线边框，浅蓝/浅绿/浅橙/浅灰配色，留白充足，布局严格对齐，不要海报感，不要 3D，不要电路板背景，不要发光，不要任何无关装饰。

文本规则必须严格遵守：
1. 所有模块名称只能使用以下英文标签，不允许新增任何别的英文词：
FPGA Top
SoC
CPU Core
Instruction Memory
Address Decode
Data Memory
MMIO Peripherals
UART
Timer
DONE
2. 所有箭头说明和注解只能使用以下中文短语，不允许新增任何别的中文句子：
取指接口
访存请求
数据存储访问
外设访问
串口输出
定时中断
结束状态
3. 禁止出现任何未在以上清单中的漂浮文字、重复标签、乱码、拼写变体。
4. 所有文字必须像排版好的矢量字体，不能像手写或模糊贴图。

构图要求：
- 最外层是一个大边界框，标题为 FPGA Top。
- FPGA Top 内部有第二层边界框，标题为 SoC。
- SoC 左侧放一个较大的 CPU Core 模块，浅蓝色。
- SoC 右上放 Instruction Memory，浅绿色。
- SoC 中间偏右放 Address Decode，浅灰色。
- SoC 右中放 Data Memory，浅绿色。
- SoC 右下放一个较大的 MMIO Peripherals 模块，浅橙色。
- 在 MMIO Peripherals 内部再整齐放置三个小模块：UART、Timer、DONE。

连接关系必须清晰：
- CPU Core 指向 Instruction Memory，箭头注解：取指接口。
- CPU Core 指向 Address Decode，箭头注解：访存请求。
- Address Decode 指向 Data Memory，箭头注解：数据存储访问。
- Address Decode 指向 MMIO Peripherals，箭头注解：外设访问。
- MMIO Peripherals 内部或右侧可用短箭头分别标示 UART、Timer、DONE 的作用，对应中文注解分别是：串口输出、定时中断、结束状态。

版面要求：
- 所有框体严格水平或垂直对齐。
- 箭头尽量横平竖直，少用弯曲线。
- 图面必须干净克制，看起来像芯片/SoC 设计说明书里的正式框图。
- 不要出现 IF、ID、EX、MEM、WB、Validation、CoreMark 等任何与本图无关的文字。

请确保最后图中只出现允许的英文标签和允许的中文注解，绝对不要多出任何一个词。
```

## 负面约束

```text
no photorealistic, no 3d render, no isometric, no futuristic HUD, no dark background, no circuit board texture, no neon glow, no glossy effect, no poster composition, no icon clutter, no handwritten text, no gibberish text, no extra floating labels, no duplicated text, no watermark
```

## 推荐输出文件名

`01-system-architecture-ai.png`
