# 图 3 验证闭环图

## 最终提示词

```text
请生成一张用于正式初赛设计说明书的验证矩阵与证据闭环图，输出为高分辨率白底 PNG，建议分辨率 2200x1400，横向构图，扁平矢量风，学术技术文档风格，深灰细线边框，浅蓝/浅绿/浅橙/浅灰配色，结构严谨，层级清晰，不要海报感，不要 3D，不要复杂背景。

文本规则必须严格遵守：
1. 所有模块名称只能使用以下英文标签，不允许新增任何别的英文词：
Smoke
RV32 Baseline
RV64 Baseline
Full UI
CoreMark
Vivado impl50
FPGA-like Probe
Validation Closure
2. 所有箭头说明和注解只能使用以下中文短语，不允许新增任何别的中文句子：
基础功能验证
覆盖范围扩展
性能验证
实现结果
近原型路径观测
统一证据链
3. 禁止出现任何未在以上清单中的漂浮文字、重复标签、乱码、拼写变体。
4. 特别禁止出现 IF、ID、EX、MEM、WB、CPU Core 等与本图无关的文字。

构图要求：
- 顶部第一层从左到右依次排列四个模块：Smoke、RV32 Baseline、RV64 Baseline、Full UI。
- 第二层居中排列三个模块：CoreMark、Vivado impl50、FPGA-like Probe。
- 第三层底部中央放一个汇聚模块：Validation Closure。

连接关系要求：
- 顶部四个模块从左到右顺序连接，体现验证覆盖逐步扩展。
- 从顶部验证链向下汇出到 CoreMark、Vivado impl50、FPGA-like Probe，形成三条支撑路径。
- CoreMark 对应中文注解：性能验证。
- Vivado impl50 对应中文注解：实现结果。
- FPGA-like Probe 对应中文注解：近原型路径观测。
- 所有路径最终汇聚到 Validation Closure，底部中文注解：统一证据链。
- 顶部顺序链可用中文概括注解：基础功能验证、覆盖范围扩展。

版面要求：
- 模块严格按三层网格对齐。
- 图面必须非常干净，不要出现任何无关缩写、额外箭头、无关字母。
- 整体效果要像正式硬件项目中的验证闭环框图，而不是宣传海报。

请确保最终图中只出现允许的英文标签和允许的中文注解。
```

## 负面约束

```text
no photorealistic, no 3d render, no isometric, no futuristic HUD, no dark background, no circuit board texture, no neon glow, no glossy effect, no poster composition, no handwritten text, no gibberish text, no extra floating labels, no duplicated text, no watermark
```

## 推荐输出文件名

`03-validation-closure-ai.png`
