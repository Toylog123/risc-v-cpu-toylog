\begin{figure}[H]
    \centering
    \begin{tikzpicture}[node distance=8mm and 10mm, every node/.style={font=\small}]
        \node[reportbox, minimum width=38mm, minimum height=43mm] (cpu) {
            \textbf{CPU Core}\\[0.4em]
            IF / ID / EX / MEM / WB\\
            Hazard / Forwarding\\
            CSR / Trap / Redirect
        };
        \node[reportbox, minimum width=31mm, right=24mm of cpu, yshift=14mm] (imem) {
            \textbf{Instruction ROM}\\
            Sync IMEM path
        };
        \node[reportbox, minimum width=31mm, right=24mm of cpu] (dmem) {
            \textbf{Data RAM}\\
            Load / Store path
        };
        \node[reportbox, minimum width=31mm, right=24mm of cpu, yshift=-14mm] (mmio) {
            \textbf{MMIO}\\
            UART / TIMER / DONE
        };

        \draw[reportline] (imem.west) -- node[above, font=\scriptsize]{同步取指} (cpu.east);
        \draw[reportline] (cpu.east) -- node[above, font=\scriptsize]{load / store} (dmem.west);
        \draw[reportline] (dmem.west) -- ++(-7mm,0) |- ([xshift=2mm]cpu.east);
        \draw[reportline] (cpu.east) -- node[below, font=\scriptsize]{外设访问} (mmio.west);
        \draw[reportline] (mmio.west) -- ++(-7mm,0) |- ([xshift=2mm]cpu.east);

        \node[reportgroup, fit=(cpu)(imem)(dmem)(mmio), label=above:{\small SoC 封装}] (soc) {};
        \node[reportgroup, thick, inner sep=16pt, fit=(soc), label=above:{\small FPGA 原型顶层}] (fpga) {};
    \end{tikzpicture}
    \caption{系统总体架构示意图}
\end{figure}
