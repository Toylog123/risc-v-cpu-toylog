\section{FPGA 原型适配情况}

\subsection{原型目标与路径}

赛题允许采用仿真与原型实现结果作为阶段性支撑，因此项目在初赛阶段将 FPGA 工作重点放在
“pre-board 可实现性确认”上，即先完成综合、实现、时序、约束和 bring-up 路径整理，再进入实体板验证。
当前原型路径以 Nexys A7-100T 为目标平台，并以 Vivado \texttt{impl50} 作为统一实现口径。

\input{figures/fpga-preboard-flow.tex}

\subsection{已完成工作}

当前已经完成的 FPGA pre-board 工作包括：
\begin{itemize}
    \item Vivado \texttt{impl50} 构建脚本、bitstream 路径和报告目录整理完毕；
    \item FPGA 顶层、XDC、bring-up checklist 和相关 README 已统一到当前主线口径；
    \item \texttt{IMEM\_OUTPUT\_REG = 0} 与 \texttt{DMEM\_OUTPUT\_REG = 0} 的原型路径配置已固定；
    \item FPGA-like probe 已用于辅助观察原型路径下的运行周期与相对效率。
\end{itemize}

\subsection{当前结果}

\begin{table}[H]
    \centering
    \small
    \caption{FPGA pre-board 关键结果}
    \begin{tabularx}{0.95\textwidth}{L{0.25\textwidth} L{0.22\textwidth} Y}
        \toprule
        项目 & 结果 & 说明 \\
        \midrule
        目标平台 & Nexys A7-100T & 当前原型验证目标板卡 \\
        统一实现口径 & \texttt{impl50} & 50MHz 路径用于资源与时序收敛 \\
        资源结果 & \texttt{2556 LUT / 2170 FF / 4 BRAM / 0 DSP} & 与主线文档保持一致 \\
        时序结果 & \texttt{WNS = +5.822ns}, \texttt{WHS = +0.057ns} & 当前 pre-board 约束下满足正裕量 \\
        原型观测结果 & \texttt{156442 cycles}, \texttt{7.728811 CoreMark/MHz} & 来自 FPGA-like probe \\
        \bottomrule
    \end{tabularx}
\end{table}

\subsection{当前边界}

需要说明的是，本文所给出的 FPGA 结果属于 pre-board 阶段结论，主要用于说明设计具备原型实现能力。
当前尚未完成的内容包括 UART/LED 实体连线验证、boot banner 现场观测以及最终板级 I/O 约束确认。
这些工作依赖实体板卡与现场条件，因此被保留为后续阶段任务，而没有在本文中被表述为已完成事项。

\subsection{阶段性结论}

从现有结果看，\texttt{YH\_rv\_cpu} 已经完成 FPGA 原型适配的关键前置工作，
具备继续进入实体板 bring-up 的工程基础。对于初赛阶段而言，这意味着方案不仅停留在 RTL 设计层面，
也已经具备面向原型实现的落地路径。
