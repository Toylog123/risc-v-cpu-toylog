# YH_rv_cpu 汇报摘要

> 日期：2026-04-09

## 一句话总结

`YH_rv_cpu` 已形成可提交的比赛冻结基线，并完成 `2026-04-08` 的 fresh 扩展验证收口。当前最重要的结果是 short CoreMark `0.912472`、strict CoreMark `0.912465` 且 `10.959325s`、`rv32 baseline 33/33`、`rv64 baseline 21/21`、`rv32 full-ui 42/42`、`rv64 full-ui 54/54`、`impl50 WNS = +5.599ns`；当前本机内主任务已转为“初赛提交材料收敛”和“redirect 成本分解后再决定是否继续优化”。

## 核心数据

| 指标 | 当前值 | 备注 |
|------|--------|------|
| 指令集 | `RV32I + Zicsr`，带 `RV64` baseline 验证 | 冻结比赛口径仍以 `RV32` 比赛流为主 |
| CoreMark short score | `0.912472 CoreMark/MHz` | short-runtime frozen score flow |
| CoreMark strict score | `0.912465 CoreMark/MHz` | strict `>=10s` validated run |
| CoreMark strict runtime | `10.959325s` | `Total ticks = 1095932534` |
| CoreMark smoke | `620530 cycles` | fresh |
| riscv-tests baseline | `rv32 33/33`，`rv64 21/21` | frozen baseline |
| riscv-tests full-ui | `rv32 42/42`，`rv64 54/54` | `2026-04-08` fresh 扩展验证 |
| impl50 资源 | `2556 LUT / 2170 FF / 4 BRAM / 0 DSP` | `project/reports/clk_20p000ns/impl_utilization.rpt` |
| impl50 时序 | `WNS = +5.599ns`，`WHS = +0.025ns` | `project/reports/clk_20p000ns/impl_timing_summary.rpt` |
| FPGA-like probe | `156442 cycles`，`7.728811 CoreMark/MHz` | quick pre-board probe |

## 已完成

- 冻结并验证当前 CoreMark short / strict 双口径
- 补齐 `rv32/rv64 baseline` 与 `rv32/rv64 full-ui` fresh 证据
- 冻结并验证 `impl50` FPGA 比赛口径与 pre-board SOP
- 明确 `fence_i` 的处理边界：扩展 UI 口径使用 `zifencei`，冻结比赛口径仍维持 `RV32I + Zicsr`
- 执行 `FQ-06A` quick-screen 并拒绝保留无收益 request-side 变体
- 已将汇报/提交材料集中到 `01-项目管理/04-汇报与提交材料/`，统一收敛提交清单、证据索引和答辩口径

## 剩余事项

- 实体板卡到位后的 UART/LED/boot banner 板级闭环
- 基于 redirect 成本分解再判断是否值得继续下一轮性能优化
- 根据 `2026-04-09` 官方答疑，初赛硬交付以设计/技术文档为主；当前性能与验证材料继续作为增强支撑
- 持续维护初赛提交材料目录，避免后续结果和对外材料再次漂移

## 风险提示

- 当前 XDC 仍不是最终板级 I/O delay signoff 版本
- `rv32 full-ui` 的 `fence_i` 通过依赖扩展 UI 口径，不应误写成冻结 ISA 升级
- 后续优化如果不做全矩阵回归，容易破坏现有冻结基线
