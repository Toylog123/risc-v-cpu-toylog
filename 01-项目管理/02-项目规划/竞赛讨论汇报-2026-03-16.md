# 竞赛讨论汇报（2026-03-16）

## 1. 当前项目做到哪一步

- 主赛题已锁定为七星微赛题，不再做选题摇摆。
- 正式工程已经统一为 `YH_rv_cpu`。
- 当前已经完成：
  - `RV32I + Zicsr` 五级流水第一版
  - 最小 SoC
  - `CSR / trap`
  - machine timer interrupt
  - `XLEN` 参数化骨架
  - `xlen64` 基础烟测
- 当前验证上已经跑通：
  - `SoC smoke`
  - `trap smoke`
  - `timer irq smoke`
  - `xlen64 smoke`
- `riscv-tests` 子集已经接入并通过最小回归
- `CoreMark` 已经接到工程里，但当前还在调通阶段
- Vivado 本地工程目录已经固定为根目录 `project/`

## 2. Git 里的当前工作状态

### 2.1 已经有的正式基线

- 远端仓库已经有：
  - 工程主线
  - 工具链说明
  - 项目管理文档
  - 交接配置 `.codex-handoff.json`

### 2.2 当前本地正在推进的内容

- `RV64` 语义继续补强
- `riscv-tests` 回归脚本和测试平台
- `CoreMark` 端口移植与烟测
- Vivado 工程脚手架和资源估算

### 2.3 当前 Git 更适合在会上怎么说

> 我们现在不是只有文档，而是已经有可运行的 CPU/SoC 基线。  
> Git 仓库里已经把工程、工具链、交接、任务和项目管理收齐，后面不管是队友接手还是老师检查，都能直接看到当前进度和下一步路线。

## 3. 当前最关键的技术结论

- 当前主线已经从“写一个 CPU 骨架”进入“做一个能交付的比赛工程”阶段。
- 当前最重要的不是再扩散方向，而是三条线同时闭环：
  - `RV64` 语义补齐
  - 验证补齐
  - FPGA 上板补齐
- 赛题明确要求 `RTL -> 仿真 -> FPGA` 完整闭环，所以没有 FPGA 板卡，项目后续会被卡住。

## 4. 当前仍然没完成的关键项

- `CoreMark` 还没有形成稳定可复现跑分记录
- 正式板卡还没到位
- 正式 XDC 还没基于实物板卡冻结
- 上板串口演示还没做
- 两项性能优化需要在文档里冻结成正式口径
- 资源报告导出还需要继续收口

## 5. 下一次会议最需要讨论和决策的事情

### 5.1 资源决策

- 是否支持采购或借用 `Nexys A7-100T`
- 最低是 `1` 块，最好是 `2` 块
- 是否能同时配齐基础线材和调试条件

### 5.2 项目决策

- 是否确认纯 FPGA 路线作为比赛展示主线
- 是否接受当前“先做完整闭环，再做深度性能优化”的节奏

### 5.3 分工决策

- 佟亚龙继续负责 CPU 主线、SoC、Vivado、板级集成
- 魏升飞继续负责 `riscv-tests`、`CoreMark`、回归记录
- 杨晨琰继续负责材料、汇报、视频和过程文档

## 6. 建议在会上直接提的需求

### 6.1 设备需求

- 推荐申请：`Nexys A7-100T`
- 最低数量：`1`
- 推荐数量：`2`
- 需要配套：USB 线、稳定供电、基础串口/逻辑观察条件

### 6.2 时间需求

- 拿到板卡后，需要尽快进入：
  - 约束冻结
  - 串口打通
  - bitstream 生成
  - 上板演示

## 7. 会上可以用的一段简短汇报口径

> 我们现在已经把 `YH_rv_cpu` 的 CPU 和最小 SoC 主线跑起来了，基础仿真是通的，验证和工程组织也已经进入可协作状态。  
> 下一阶段的核心瓶颈不再是“有没有代码”，而是“能不能进入 FPGA 板级闭环”。  
> 所以这次最需要老师支持的是板卡资源，建议优先给我们一块 `Nexys A7-100T`，最好两块，这样我们能把串口演示、频率和资源统计真正做出来，后面再把 `CoreMark` 和提交材料收口。
